apiVersion: v1
kind: Service
metadata:
  name: {{ include "OrderServiceChart.fullname" . }}
  labels:
    {{- include "OrderServiceChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 9080
      protocol: TCP
      name: http
  selector:
    {{- include "OrderServiceChart.selectorLabels" . | nindent 4 }}
