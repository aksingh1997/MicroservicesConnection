apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "UserServiceChart.fullname" . }}-test-connection"
  labels:
    {{- include "UserServiceChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "UserServiceChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
